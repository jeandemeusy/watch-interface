## Summary

What does this PR do?

## Changes

-

## Testing

How was this tested? Include any relevant commands run.

```bash
./watch_interface_statistics.sh collect --once
```

## Checklist

- [ ] Tested `collect`, `trends`, and `distribution` subcommands manually
- [ ] Existing CSV schema is preserved (or a migration path is documented)
- [ ] `--help` output updated if new flags were added
